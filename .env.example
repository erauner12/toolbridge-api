# ToolBridge API Configuration
# Copy this file to .env and update the values for your environment

# =============================================================================
# Application Environment
# =============================================================================
ENV=dev                           # dev, staging, production

# =============================================================================
# Server Configuration
# =============================================================================
HTTP_ADDR=:8080                   # HTTP server bind address (e.g., :8080 or 0.0.0.0:8080)
API_PORT=8080                     # External port for Docker Compose

# =============================================================================
# Database Configuration
# =============================================================================
# Full DATABASE_URL (recommended for production)
DATABASE_URL=postgres://toolbridge:dev-password@localhost:5432/toolbridge?sslmode=disable

# Or individual components (for development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=toolbridge
DB_NAME=toolbridge
DB_PASSWORD=dev-password

# PostgreSQL admin password (for Docker Compose)
POSTGRES_PASSWORD=dev-password

# =============================================================================
# Authentication & Security
# =============================================================================
# JWT signing secret (MUST be changed in production!)
# Minimum 32 characters recommended
JWT_HS256_SECRET=dev-secret-change-in-production

# =============================================================================
# Migration Configuration
# =============================================================================
MIGRATIONS_DIR=migrations         # Directory containing SQL migration files

# =============================================================================
# Docker Compose Configuration
# =============================================================================
# These are used by docker-compose.yml

# API service port mapping (host:container)
# API_PORT is defined above

# Postgres volume (optional, defaults to named volume)
# POSTGRES_DATA_DIR=/path/to/postgres/data
