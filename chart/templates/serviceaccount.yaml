{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "toolbridge-api.serviceAccountName" . }}
  labels:
    {{- include "toolbridge-api.labels" . | nindent 4 }}
{{- end }}
